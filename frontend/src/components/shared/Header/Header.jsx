import React from 'react';

import { Navbar } from 'Shared';

import './Header.scss';

export default () => {
    return (
        <div className="Header">
            <Navbar />
        </div>
    );
};
