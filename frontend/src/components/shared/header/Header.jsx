import React from 'react';
import Navbar from '../Navbar';

import './Header.scss';

export default () => {
    return (
        <div className="Header">
            <Navbar />
        </div>
    );
};
