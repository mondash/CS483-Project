import React from 'react';
import Layout from '../components/shared/Layout';

const Organization = () => {
    return(
        <Layout>
            <h1>Organization</h1>
        </Layout>
    );
};

export default Organization;
